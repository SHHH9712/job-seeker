import SigninButton from "./SigninButton";

const MenuBar = () => {
  return (
    <div className="flex flex-row bg-slate-900 itmes-end justify-end p-5">
      <SigninButton />
    </div>
  );
};

export default MenuBar;
